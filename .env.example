# Copy to .env for `bun run dev:cf`. Wrangler loads it as a Worker binding; Vite
# only exposes VITE_-prefixed names to the client, so this never reaches the browser.
# Never commit the real value: anyone holding this URL can post to the channel.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/000000/xxxxxxxx
